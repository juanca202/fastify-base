NODE_ENV=development
HOST=0.0.0.0
PORT=3000
LOG_LEVEL=info

OTEL_SERVICE_NAME=fastify-base
OTEL_SDK_DISABLED=false
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318

# Punto de enganche: autenticación (src/plugins/auth.ts). No hay mecanismo en la base.
# Las variables del mecanismo, cuando exista, viven aquí.

# Punto de enganche: persistencia (src/plugins/database.ts). No hay motor en la base.
# Las variables del motor, cuando exista, viven aquí.
